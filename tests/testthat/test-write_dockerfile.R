test_that("Writing Dockerfile works", {
  # This test cant run till I have a folder with a git root
  # So perhaps I can clone a test project, or git init inside of one
  # Then create a Dockerfile
})
